package dao;

import java.util.List;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

import model.Account;
import model.Client;

public class PayServDAO {
	
	public List<Client> getAllClients() {
		return this.getEntityManager().createQuery("select c from model.Client c", Client.class).getResultList();
	}
	
	public List<Account> getAllAccounts(int clientId) {
		return this.getEntityManager().createQuery("select a from model.Account WHERE a.idClient LIKE :id", Account.class).setParameter("id", new Integer(clientId)).getResultList();
	}
	
	private EntityManager getEntityManager() {
		EntityManagerFactory emf = Persistence.createEntityManagerFactory("Payment Services");
		EntityManager em = emf.createEntityManager();
		return em;
	}
}
